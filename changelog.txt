- Changelog (newest first)

1.1.6 (2025-12-16, commit f9f415b)
- Auto-direction scale-ins now set `reduceOnly=False` (previously inherited reduce flag), preventing accidental instant closes when adding to an aligned position.

1.1.7 (2025-12-16, commit pending)
- Prevent cleanup from cancelling reduce-only protection orders when a position was seen during the cycle (guards partial opens from losing SL/TP). Also stdout/stderr is now mirrored into the rotating main log.

1.1.8 (2025-12-16, commit pending)
- Treat Bybit 110017 (“current position is zero, cannot fix reduce-only order qty”) as a benign info case for extra orders instead of logging it as an error, avoiding noisy error spam when a reduce-only ladder overshoots residual size.

1.1.5 (2025-12-16, commit 1851276)
- Fixed protection restore crash (`entry_price` unbound) so stop/TP recalculation no longer aborts and keeps positions from being closed unnecessarily.

1.1.4 (2025-12-16, commit 0fcad9a)
- Fixed an AUTODIRECTION env dependency at import time (no more `env_bool` NameError on fresh boots) and tuned the fallback loader to prefer `origin/stable` over the local `stable` branch when materializing backup scripts.

1.1.3 (2025-12-16, commit e5848b4/9800fd0)
- Added directional auto-adjustments: opposing AI signals automatically trim or flip exposure while aligned signals scale into the trade, gated by confidence and configurable scale/reduce factors.
- Anchored ATR-based stops and take-profits to the entry (when available), close immediately if the price has already violated the computed level, and log the breaching symbols for the SQLite protection history.
- Added optional AI request context slimming plus a dedicated UTF-8 `assets/error.log` that now mirrors the raw console warnings/errors.

1.1.2 (2025-12-15, commit pending)
- Added a volatility-aware balance between news and technical analysis so the AI leans on catalysts in high ATR conditions and defaults to TA in calm markets.
- Spot SELLs now execute against available balances (no blanket short block); skips only when balance is insufficient.
- Highlighted open/skip decisions in the logs with regime/analysis balance details, SL/TP multipliers, reason for skipping, and any extra logic guiding the entry.

1.1.1 (2025-12-15, commit pending)
- Embedded the structured market-mode JSON guide into symbol prompts, scaled risk per regime, and enforced the new confidence floors so trend/countertrend/range behaviors match the documented rules.

1.1.0 (2025-12-12, commit pending)
- Reduce-only stop/stop-limit/trigger orders now force triggerDirection based on the live ticker price when closing positions, preventing Bybit `110092/110093` rejections like the DOGE/USDT stop error observed at 17:40 UTC+03.

1.0.9 (2025-12-12, commit pending)
- Extra order trigger directions now fallback to the live ticker price when no reference was provided, eliminating Bybit `110092/110093` errors on reduce-only stops such as LTC/USDT.

1.0.8 (2025-12-12, commit pending)
- Changelog parsing now reads the first (newest) entry, fixing the backup mode loop that occurred after fresh commits.
- Guarded `selection_style` so plan builds without a universe response no longer crash on `UnboundLocalError`.

1.0.7 (2025-12-11, commit pending)
- Fixed an indentation error in universe metadata application so selection indicators/timeframes no longer crash the loader.

1.0.6 (2025-12-11, commit pending)
- Next-cycle scheduling now honors the universe-provided UTC+03 timestamp instead of relying on a delay, so the runtime sleep resumes exactly on the requested start time.

1.0.5 (2025-12-11, commit pending)
- Universe instructions now request style/aggression/horizon/news/indicator/timeframe metadata (next run also in UTC+03) and the resulting values feed straight into the initial prompts/bundles so the AI respects the desired intraday posture.

1.0.4 (2025-12-11, commit pending)
- Extra-order sizing now reads snake_case fields such as `amount_percent`/`size_pct`, so reduce-only instructions from the AI no longer get skipped for “Unable to determine amount”.

1.0.3 (2025-12-11, commit pending)
- Reduce-only stop orders now force the correct trigger direction (falling for long exits, rising for short exits), fixing the Bybit `retCode 110092` rejection observed on ETH/USDT extra orders.

1.0.2 (2025-12-11, commit pending)
- Clarified to the AI prompt that we trade perpetual futures, can go long or short, and should adjust current exposure (scale/add, trim, close, or flip) based on the expected directional bias.

1.0.1 (2025-12-11, commit pending)
- Prevented `_refresh_position_protection_if_possible` from raising `trailing_mult` reference errors by precomputing the trailing settings before matching existing protective orders.
- Adjusted the public release metadata to describe the latest resilience fixes.

1.0.0 (2025-12-11, commit pending)
- Switched the release counter to semantic MAJOR.MINOR.PATCH values so future versions are no longer tied to calendar dates.
- Centralized the protection refresh logic that runs for manage/hold/open paths, giving every active position another chance to reapply stops before fallback closures fire.

2025.12.07.4 (2025-12-07, commit pending)
- Prompt now explicitly ranks regime priorities (trend > flat > counter) while keeping long/short neutral and countertrend allowed.

2025.12.07.3 (2025-12-07, commit c6cb840)
- Prompt wording neutralized: trend-following remains preferred, countertrend is low priority, and long/short are treated symmetrically with no side bias.

2025.12.07.2 (2025-12-07, commit c330b4c)
- Prompt adjusted to favor trend-following entries by default and treat countertrend setups as lower priority unless strongly confirmed; log-rotation change from 12.07.1 is kept.

2025.12.07.1 (2025-12-07, commit f2adb40)
- Log rotation keeps all archived bybit logs without deleting older files; external tools (logrotate, manual cleanup) can manage long-term retention.

2025.12.07.0 (2025-12-07, commit b29c6ef)
- Log improvements: /logs can now return up to 500 lines, manage summaries explicitly mark symbols with no open position, and results_state diagnostics stay enabled for closed-order debugging.

2025.12.05.5 (2025-12-05, commit a2d663a)
- Expanded results_state diagnostics: logs now show how many closed orders were fetched into the PnL window and which IDs were treated as new for this cycle.

2025.12.05.4 (2025-12-05, commit b1f7108)
- reduce-only extra orders now flip to the closing side (buy for shorts, sell for longs) before hitting Bybit so "same side as current position" rejections vanish.

2025.12.05.3 (2025-12-05, commit 251a9f2)
- Sorted the changelog chronologically, removed duplicate sections, and added commit/date metadata for recent releases.

2025.12.05.2 (2025-12-05, commit 8040030)
- execute_extra_orders now builds the prompt summary from the current position payload (no more raw_position_payload NameError), keeping manage/extra-order flows alive across symbols.

2025.12.05.1 (2025-12-05, commit 3de8cf9)
- Fixed the AI decision prompt context so margin/position summaries are always present (resolving the `position_summary` NameError) and ensured master-position sharing logic feeds every request.
- analyze_graphs.py now discovers state files in both `assets` and script directories (plus `BYBITBOT_STATE_DIR`), plots closed PnL on a proper datetime axis, and logs coverage for the signal/action distribution chart.
- Initial AI context now normalizes configured timeframes, only loads those slices (two for intraday) and trims their depths in order of priority, with log messages showing the actual TF counts; indicator trimming still kicks in after depth limits. 30m/4h hard-coding removed.
- `.env` tuned for balanced intraday style: primary TF 1h with secondary 4h, deeper histories (72/36 bars), and an eight-indicator priority list `[ema20, ema50, ema200, vwma20, atr14, rsi14, macd, stoch14]` shared between BASE and initial pools; needs-depths updated to 16/10/8 bars.

2025.12.05.0 (2025-12-05, commit b82a4bd)
- Master bot is now the sole OpenAI caller: its per-symbol decisions are cached to disk and reused by follower bots while prompts/logs regained readable ASCII text (fixes the missing `position_summary` NameError).
- analyze_graphs.py plots equity against actual timestamps with date grids, reuses equity data for PnL when needed, and falls back to ai_decisions.log for signal distribution so all three charts render even without results_state.json.
- Added master-decision persistence controls and warning logs so follower cycles skip AI calls gracefully whenever no fresh master snapshot is available.

2025.12.04.1 (2025-12-04, commit n/a)
- Initial AI context now always starts from a single prioritized indicator list in .env (up to eight indicators per TF), sends OHLCV plus this set where possible, and trims tokens by reducing timeframe depth first and then dropping the lowest-priority indicators while keeping core EMAs.

2025.12.02.6 (2025-12-02, commit n/a)
- Regime now adjusts per-symbol SL/TP multipliers and notional scaling (trend keeps full size, flat/counter tightens targets and reduces size), logs include regime/indicators/risk tweaks, and initial context keeps a minimal 30m slice under token trimming.

2025.12.02.5 (2025-12-02, commit n/a)
- Regime detection (TREND_UP/TREND_DOWN/FLAT/COUNTER) is computed algorithmically, logged with each symbol, and passed into ai_decision along with the active indicator set for better model guidance.

2025.12.02.4 (2025-12-02, commit n/a)
2025.12.02.3
- Logs now report the latest close/EMA20/EMA50/RSI/ATR before making an AI decision, improving indicator visibility.

2025.12.02.2
- Trailing-stop detection now references target parameters after they are available, preventing missing-variable errors when restoring protection for managed positions.

2025.12.02.1
- Trailing stop is applied only when explicitly requested by the model and only for open positions; tightened SL/TP detection remains in place.

2025.12.02.0
- Stop protection cleanup keeps newly placed stops and retries stop creation if cleanup leaves none, avoiding false missing protection closes and ensuring stops stay on positions.

2025.11.17.1
- Support answers now search the entire repo (sans secrets) with multilingual keywords, so /support can explain trading logic, sandboxes, etc.
- README documents AI model env vars and clarifies that each trader runs on their own Bybit balance.

2025.11.17.0
- Telegram help messages are now pinned in the Commands topic with structured sections.
- /logs supports symbol/time filters (e.g. /logs BTC 60) and the bot can pull ticker-specific logs for the last hour.
- Added a detailed workflow for onboarding new traders without sharing API keys and enabled verbose exchange order logging.

2025.11.14.0
- Telegram log mirroring replicates console output to Telegram with batching and rate limiting to avoid 429 errors.
- /ai payload diagnostics make it easy to inspect the latest OpenAI request/response.

Changelog (newest first)

11.7
- Force SOL (and other small-sized positions) to regain protection: fallback TP now places a full-quantity limit when partial layers fall below exchange precision, and `set_trading_stop`/trailing fallbacks carry both `trailingStop` and `trailingAmount` parameters.
- Aggregated TP warnings now report why placement failed (min precision, notional, API errors) instead of silent skips.
- Engine notifications now stay in Releases/Git topics while trade bots post to Trade Log/Results/Status with per-user nicknames (default `redfaraon`).
- Added `/bybitkey` Telegram command and persisted credential store so every user can supply or clear their own Bybit API key/secret without editing `.env`.

11.6
- Introduced multi-user profiles with per-trader state, secrets, and Telegram prefixes; run `python bybitbot.py --user <id>` to launch an isolated session.
- Added `users/users.example.json` and per-user secret files so traders supply their own Bybit API keys while OpenAI/Telegram tokens stay shared.
- State files (results, equity, release/fallback history) now live under each user's runtime directory, preventing cross-account bleed.

11.5
- Release topic now de-duplicates announcements, keeping a single message per new version or commit by persisting the last broadcast in `release_state.json`.
- Added groundwork for multi-tenant support by planning secure API separation and optional per-user runtimes.

11.4
- Hardened position protection so short positions always regain take-profit coverage, using `set_trading_stop` fallbacks and trailing-stop retries when limits are too small.
- Unified Trading status now reports only today's (UTC) Unified↔Funding transfers and hides direct Funding account activity entirely.
- Reorganized this changelog for clarity with a single newest-first timeline.

11.3
- Send 6-hour PnL summaries and newly closed order breakdowns to the Results topic (ID 22196) with deduped reporting and state persistence.
- Push open-position snapshots to the Status topic (ID 22198) each cycle for quick monitoring.
- Added new Telegram topic configuration knobs and git change tracking to highlight local work-in-progress.
- Auto-pull now hard-resets to origin if history diverges (keeps servers synced after force pushes).
- Unified Trading telemetry now reports last-24h transfers between Unified and Funding only (no per-order listing).


11.2
- Added Telegram `/schedule` command that can schedule, reschedule, or cancel the next cycle directly from the webhook with immediate override support.
- Added `/tokens` webhook command to inspect and adjust OpenAI token budgets (cycle soft limit, hard stop, cheap-model threshold) and persist updates to `.env`.
- Reload `.env` values on every cycle, providing new defaults for release topic id and OpenAI budgets so manual edits take effect immediately.
- Announce new commits and releases in the Releases topic (ID 7) with commit timestamp, hash, message, version, and changelog details.


2025.10.31.03
- Added Telegram webhook command handling with buffered log forwarding and topic fallbacks.
- Made Telegram sending thread-safe, batching log flushes, and reconfigured after env reload.
- Documented new Telegram and trailing stop environment switches and aligned loader version metadata.


2025.10.31.02
- Added dynamic trailing-stop tightening based on ATR profit distance to lock in gains with less whipsaw.


2025.10.31.01
- Fixed cycle counter init so start banner shows correct number and prevents fallback launch failures.


2025.10.30.03
- Reduced per-pair serialization (bars/timeframes) and cycle symbol cap to fit OpenAI limits without hurting signal quality.
- Added explicit cycle number logging and skipped legacy branch probes during routine backups.


2025.10.30.02
- Excluded legacy branch from routine backup rotations to prevent surprise launches on outdated code.


2025.10.30.01
- Logged active branch and commit timestamp each cycle and shared 6h PnL snapshots at session start/end.
- Persisted real trade-cycle counters to align fallback probes with actual bot runs.
- Detailed redundant stop cleanup messages with order specs and switched trailing protection to Bybit trading-stop API.


2025.10.28.10
- Allowed scale-in extra orders and preserved staged ladders when cleaning limits.
- Switched trailing-stop params to trailingAmount for Bybit USDT Perps to avoid API errors.


2025.10.28.9
- Preserved ladder/scale-in limit orders during cleanup to keep staged entries intact.


2025.10.28.8
- Fixed NameError when launching random/routine backups by guarding suppress flags and passing them explicitly.


2025.10.28.7
- Added ATR-based break-even stop adjustments with configurable thresholds, improving downside protection once trades move in profit.
- Expanded dynamic risk controls: telemetry now respects break-even states and context shares current risk window with AI.


2025.10.28.6
- Added dynamic risk scaling driven by recent PnL, with guardrails and logging for cycle risk posture.
- Shared risk telemetry in AI context payload to inform decision prompts.
- Logged per-cycle risk state and ensured baseline recovery when no PnL data is available.


2025.10.28.5
- Defined MAX_POSITIONS_PER_BASE before settings load to fix NameError on initial import.


2025.10.28.4
- Tied routine counter to completed cycles and suppressed child-script increments so scheduled backups no longer loop.
- Propagated suppress flags via environment for fallback/random launches to keep latest commit active.


2025.10.28.3
- Auto-discovered Bybit symbol aliases (e.g., 1000 tickers) to avoid missing market errors.
- Added base-asset exposure guard to cap concurrent positions per coin and prevent over-leveraging.
- Generated daily AI decision summaries in assets and refreshed PnL analytics with closed-trade data.


2025.10.28.2
- Added SHIB perpetual alias to avoid missing market errors when AI selects SHIB/USDT.
- Switched PnL reporting to sum closed orders over the 6h window, with trade history fallback.
- Version bump for loader/impl.


2025.10.28.1
- Enforced Bybit order sizing to honor symbol min quantity/notional, eliminating 110043 rejections.
- Added EMA/RSI fallback decisioning when OpenAI token limits hit so cycles still trade instead of skipping.
- Cached exchange trade rules per run and bumped version metadata.


2025.10.27.7
- Restore GPT-4.1-mini as the primary model until 70k tokens and keep GPT-4o-mini as the fallback per cycle.
- Reapply AI-provided stop/take targets when skipping opens and improve trailing-stop placement defaults.
- Analyse up to six new universe symbols per run in addition to current positions.


2025.10.27.6
- Process the entire AI universe each cycle so up to 8 symbols are analysed even when the list exceeds prior limits.
- Refresh protective TP/SL orders whenever an open signal lands on an already-held position.
- Fix triggerDirection calculations using live prices and drop the unused stable_impl fallback copy.


2025.10.27.5
- Detect failed open orders and mark them as such in session summaries, surfacing the exchange error text.
- Log planner aliases but avoid claiming a position opened when the order was rejected.
- Version metadata bumped for loader, impl, and stable builds.


2025.10.27.4
- Skip replacement reduce-only market orders lacking triggers to prevent unintended flat exits.
- Normalize planner actions such as replace_orders into manage/hold flows and log the mapping.
- Updated default version metadata in loader and implementation.


2025.10.27.3
- Normalized Bybit extra-order payloads and updated version metadata.


2025.10.26.2
- `_sync_with_remote` now runs `git fetch`/`git pull --ff-only --autostash` even on dirty trees and reports every git action to a dedicated Telegram topic (ID 581 by default).
- Added `_send_git_notification` helper so commit banners and git outputs always include timestamp/message and land in the git thread.
- Telegram thread failures (400 thread not found) now fall back to the main chat automatically, preventing stuck retries.


2025.10.26.1
- Universe prompt now re-generates every run from scratch using session time/day, news-only context, and a curated TF/indicator shortlist (2x EMA, vol, RSI, MACD, ATR, Stoch).
- Initial per-symbol context is clamped to 30m/4h + those six indicators with ~10 bars, while extra needs pass through whatever the model requests.
- Telegram notifications gained duplicate suppression, minimum interval, topic-aware routing, and retry/backoff handling (avoiding 429s); logs now include commit timestamp + version.


2025.10.24.1
- Universe prompt now rebuilds every cycle with a curated shortlist (EMA20/EMA50, vol, RSI, MACD, ATR, Stoch; TF 15m-4h) and includes session timestamp/day/hour.
- Initial context always uses compact 30m/4h + six indicators; extra needs honour model requests with a 10-bar cap.
- Telegram logging now includes commit timestamp + version, uses thread IDs, rate-limits messages, de-duplicates repeats, and retries gracefully on 429/transport errors.


2025.10.23.3
- Added TELEGRAM_TOPIC_ID support and thread-aware send_tg helper so alerts land in the desired group topic.
- Clamped portfolio needs to a fixed shortlist (30m/4h, ema20/ema50/vol/rsi14/macd) and trimmed context to 10 bars for leaner prompts.
- Misc tuning for low-confidence follow-ups and version bump to keep loader/changelog in sync.


2025.10.22.7
- Added low-confidence telemetry: per-symbol confidence logged, extra context auto-requested when below threshold, with configurable needs list.
- Skipped re-creating existing manage orders and canonicalized order payloads to prevent invalid-price/qty errors.


2025.10.22.6
- Fixed manage/hold extra orders: default to reduce-only, allow additive scaling only with explicit intent; normalized ticker aliases across data fetches to prevent BNB/POL lookup errors.


2025.10.22.5
- Gracefully handles KeyboardInterrupt by stopping without triggering legacy fallbacks, ensuring manual interruptions remain safe.


2025.10.22.4
- Hardened OpenAI retries with exponential backoff and optional probe attempts to restore the latest commit after fallbacks.
- Normalized ticker aliases (e.g. BNB, POL) across data fetches and order management so Bybit lookups always use canonical symbols.
- Tightened extra-order safeguards: limit non-reduce duplicates, enforce margin checks, and prune stale limit orders before placing new ones.


2025.10.22.3
- Hardened AI retries and logging so fallbacks clearly state source and target, and cycles fail fast when the trade plan is unavailable.
- Normalized ticker aliases for extra-symbol bundles so symbols like `BNB` always fetch candles/orders from Bybit.
- Recognize protective orders even without the `reduceOnly` flag and drop non-reduce extra orders that would expand existing exposure.


2025.10.22.2
- Launcher now falls back to the dedicated `stable` branch whenever the latest commit fails validation, keeping prior commit/backups as secondary safeguards.
- Trading cycle aborts when every flat symbol is skipped by the trade plan or when open positions lack stop/trailing protection, triggering an automatic rollback to stable.


2025.10.20.6
- Лимит открытых позиций задаёт модель: используем значение из первого запроса вместо жёсткого конфига.


2025.10.20.5
- Модель задаёт объёмы: используем переданные лоты/нотионал при открытиях, сохраняя маржинальные проверки.


2025.10.20.4
- Модель управляет плечами: применяем переданные значения и учитываем их при маржинальных расчётах.


2025.10.20.3
- Перешли на CryptoCompare API с RSS-фолбэком и убрали CryptoPanic, избегая 429.
- Во втором запросе теперь передаются полные тексты новостей, заголовки остаются краткими для первого шага.


2025.10.20.2
- Сделан торговый план основным источником решений: теперь за цикл отправляется один многопарный запрос без индивидуальных initial-запросов.
- Позиции вне плана переводятся в hold/skip с безопасной защитой и явной причиной.
- В payload торгового плана добавлены снапшоты открытых позиций и ожиданий по ордерам для всей выборки.


2025.10.20.1
- Validate model/portfolio symbols against live Bybit markets, auto-alias MATIC→POL, and warn on delistings.
- At cycle end, re-establish missing SL/TP protection or close the position if safeguards cannot be restored.
- Loader marks new commits stable only after a successful protected run and preserves readable English logs.


2025.10.19.3
- Added automated protection validation so new builds only become stable when SL/trailing orders are present.
- Boot loader now treats the first successful run of a commit as stable and avoids reusing failed fallbacks.
- Added alias handling for Bybit symbol changes (e.g., POL replaces MATIC) and normalized pair checks.


2025.10.19.2


2025.10.19.1
- 2025-10-19 e5df32c refactor: enhance runtime status management and default delay configuration
- 2025-10-19 d24c0b5 refactor: enhance order type handling with aliases and normalization function
- 2025-10-19 0f28cd5 refactor: update sync_script.sh for targeted synchronization of project files and directories
- 2025-10-19 c508f72 refactor: streamline sync_file function and enhance synchronization logic
- 2025-10-19 d51a298 Add sync_script.sh for synchronizing local and remote directories
- 2025-10-19 95ea8af docs: restore cron autostart workflow
- 2025-10-19 d05355f 2025.10.19.1: rollback and fix needs handling
- 2025-10-19 7f118eb Initial commit (recovered)


2025.10.18.1
- Portfolio prompt now includes a symbol_configs block so the model can define pair-specific risk and sizing settings.
- Selection parsing merges symbol_configs into targets and broadcasts the applied per-symbol parameters.
- Execution layer combines base pair configs with per-decision overrides and logs the effective inputs used for orders.
- Extra analysis timeout path now returns a safe skip decision immediately to avoid control flow bugs.


2025.10.17.2
- Restored safe default pools for pairs/indicators/timeframes so the model always gets a tradeable universe with news context.
- Fallback amount logic ensures model-provided protective orders place even when using percent sizing.
- Runtime status file records the next scheduled wake-up for monitoring.
- Automatic universe refresh asks the model for updated default pools using news context.
- Protective SL/TP/trailing orders are enforced for existing positions if missing.
- Added manage_update.py to automate scheduled or immediate updates aligned to HH:01 / HH:31 restarts.
- README rewritten with quick start and cron instructions for manage_update.py workflow.
- manage_update.py now reads runtime_status.json to restart at the model-planned next_run time when available.
- Model now receives config_defaults (.env risk/leverage/ATR settings) and can recommend overrides; overrides are sanitized, logged, and applied per-cycle.


2025.10.17.1
- Portfolio prompt now shares default scheduling inputs and enforces next_run_minutes so the bot self-reschedules without cron.


2025.10.16.18
- Model must nominate symbols/timeframes/indicators directly from news; defaults removed and next_run_minutes requested.
- Session banners and Telegram announcements now report Equity, Free margin, and unrealised PnL, noting fallback builds when used.


2025.10.16.17
- Loader fallback skips backup builds with trailing "B" (beta).
- Version value is maintained in CHANGELOG.txt and propagated by loader/impl.
- Legacy backup files named bybit_intraday_30m_5pairs_v*.py are now detected alongside new bybitbot_v*.py snapshots.


2025.10.16.16B
- Expanded default pools (PAIR_UNIVERSE / INDICATOR_POOL / DEFAULT_TIMEFRAMES) with optional env overrides.
- Forced review of symbols with open positions or orders; session banners honour timezone; order checks now use safe_float.
